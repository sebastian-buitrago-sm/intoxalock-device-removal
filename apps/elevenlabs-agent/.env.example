# Secret credentials for the ElevenLabs agent CLI.
# Copy to `.env` (git-ignored) and fill in. Non-secret, per-environment values
# (agent_id, phone_number_id, webhook URL, ...) live in config/<env>.toml instead.

XI_API_KEY=
